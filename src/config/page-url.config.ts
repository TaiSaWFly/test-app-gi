/**
 * PAGEURLS - application configuration pages map
 * @class
 */

class PAGEURLS {
  private root = "/";

  MAIN = this.root;
  REPOSITORY = `${this.root}repository/`;
}

export const APP_PAGES = new PAGEURLS();
